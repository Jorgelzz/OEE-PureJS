// Pegar valores do DOM corretamente, estar dentro de um evento

document.addEventListener('DOMContentLoaded', Calculate =>{
    
    let availabilityMachine = document.getElementById('availability');
    let productivityMachine = document.getElementById('productivity');
    let qualityMachine = document.getElementById('quality');

})

